const initialState = {
  isLoading: false,
  isError: false,
  basket: [],
};
const basketReducer = (state = initialState, action) => {
  switch (action.type) {
    case "x":
      return state;

    case "x":
      return state;

    default:
      return state;
  }
};

export default basketReducer;
